# InstantDB public app ID (safe to commit — this is a client-side ID)
PUBLIC_INSTANT_APP_ID=your-instantdb-app-id-here

# InstantDB admin token (NEVER commit this — used for schema push only)
INSTANT_ADMIN_TOKEN=your-admin-token-here
